# Squelch Proposals

This branch hosts the head reference for Bundle 3 GitHub PRs proposed by
the `| squelch mode="tune"` pipeline. Each PR is documentation only —
SPL diffs land in the PR body, not as a file change. Bundle 4 upgrades
to real per-detection branches with actual SPL file commits.
